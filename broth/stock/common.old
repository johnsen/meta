# Live-helper options
# These options are not supported when called from lh_config
# Why, oh whyyy?

# $_DEBUG: enable debug
# (Default: enabled)
_DEBUG="enabled"

# Internal stuff (FIXME)
APT_OPTIONS="--yes --force-yes"
APTITUDE_OPTIONS="--assume-yes"
